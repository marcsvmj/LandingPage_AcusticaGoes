# Normalize line endings
* text=auto

# Treat all images as binary
*.png binary
*.jpg binary
*.gif binary
*.svg binary

# Optional: treat minified JS/CSS as binary (to avoid messy diffs)
*.min.js binary
*.min.css binary
